<div class="container">
  <div class="sm-12 playlist-show">
  <h1><%= @playlist.name %></h1>
    <ul>
      <% @playlist.songs.each do |song| %>
        <li><%= song.title %> - <%= song.artist %></li>
      <% end %>
    </ul>
    <%= link_to "Add a new song", new_song_path%>
  </div>
</div>
